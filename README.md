# leaf-segmentation-and-classification
I trained my own models using existing architectures. I used DeepLabv3 for segmentation and trained it on the CVPPP dataset, and I trained an SVM classifier using features extracted from the Flavia dataset.

*Make sure to install CVPPP Dataset and Flavia datsets from kaggle/appropriate sources*
